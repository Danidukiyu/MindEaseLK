const { pool } = require('./db');
pool.query("SELECT id, username, email FROM users")
  .then(res => {
    console.log(res.rows);
    process.exit(0);
  })
  .catch(err => {
    console.error(err);
    process.exit(1);
  });
